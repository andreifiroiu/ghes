*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# Written per host by `php artisan seo:publish-robots` from that host's .env,
# so nginx serves it as a 200. Committing it would ship one host's policy to
# every other one — including `Disallow: /` onto production.
/public/robots.txt
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
/.playwright-mcp

# Ralph loop session state
.claude/*.local.md
